rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // NyxPulse uses Firebase Admin on the server for all reads/writes.
    // Deny all direct client access by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
